import styled from 'styled-components';

export const Input = styled.input`
  width: 100%;
  border: none;
  background-color: rgb(240, 240, 240);
  padding: 8px 10px;
  border-radius: 5px;
  font-size: 16px;
  width: 360px;
  &:focus {
    outline: 0;
  }
`;
