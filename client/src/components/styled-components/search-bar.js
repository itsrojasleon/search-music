import styled from 'styled-components';

export const Input = styled.input`
  width: 50%;
  border: none;
  background-color: rgb(240, 240, 240);
  padding: 10px 10px;
  border-radius: 5px;
  font-size: 18px;
  &:focus {
    outline: 0;
  }
`;
